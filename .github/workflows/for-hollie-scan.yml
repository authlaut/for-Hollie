name: For Hollie legacy scanner (disabled)
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "For Hollie v8.4.0 uses .github/workflows/for-hollie-browser-scan.yml"
