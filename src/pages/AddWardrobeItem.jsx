import { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, Save, Link as LinkIcon } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import Header from '../components/Header'
import ProductImageManager from '../components/ProductImageManager'
import { supabase, supabaseConfigured } from '../lib/supabase'
import { useAuth } from '../context/AuthContext'
import { taxonomy } from '../lib/taxonomy'
import '../wardrobe.css'

const initial={custom_name:'',retailer_id:'',brand:'',category:'Tops',subcategory:'T-Shirts',color:'',size:'',purchase_price:'',regular_price:'',purchase_date:'',status:'owned',product_url:'',image_url:'',image_gallery:[],notes:''}
export default function AddWardrobeItem(){
 const{session}=useAuth();const navigate=useNavigate();const[form,setForm]=useState(initial),[retailers,setRetailers]=useState([]),[profileId,setProfileId]=useState(null),[saving,setSaving]=useState(false),[message,setMessage]=useState('')
 useEffect(()=>{(async()=>{if(!supabaseConfigured||!session?.user?.id)return;const[{data:p},{data:r}]=await Promise.all([supabase.from('fh_profiles').select('id').eq('owner_user_id',session.user.id).single(),supabase.from('fh_retailers').select('id,name,slug').eq('enabled',true).order('scan_priority')]);setProfileId(p?.id||null);setRetailers(r||[])})()},[session?.user?.id])
 const subs=useMemo(()=>taxonomy[form.category]||[],[form.category]);const change=(k,v)=>setForm(p=>({...p,[k]:v}));const categoryChanged=v=>setForm(p=>({...p,category:v,subcategory:taxonomy[v]?.[0]||''}))
 const applyImported=d=>{setForm(p=>({...p,custom_name:d.name||p.custom_name,brand:d.brand||p.brand,category:d.category||p.category,subcategory:taxonomy[d.category]?.includes(p.subcategory)?p.subcategory:(taxonomy[d.category]?.[0]||p.subcategory),color:d.color||p.color,regular_price:d.regularPrice??p.regular_price,purchase_price:p.purchase_price||d.currentPrice||'',product_url:d.canonicalUrl||p.product_url,retailer_id:d.retailer?.id||p.retailer_id,image_url:d.images?.[0]||p.image_url,image_gallery:d.images||p.image_gallery}))}
 async function submit(e){e.preventDefault();setMessage('');if(!profileId||!session?.user?.id){setMessage('Your profile is unavailable.');return}if(!form.custom_name.trim()){setMessage('Product name is required.');return}setSaving(true);const payload={owner_user_id:session.user.id,profile_id:profileId,retailer_id:form.retailer_id||null,custom_name:form.custom_name.trim(),brand:form.brand.trim()||null,category:form.category,subcategory:form.subcategory||null,color:form.color.trim()||null,size:form.size.trim()||null,purchase_price:form.purchase_price===''?null:Number(form.purchase_price),regular_price:form.regular_price===''?null:Number(form.regular_price),purchase_date:form.purchase_date||null,status:form.status,product_url:form.product_url.trim()||null,image_url:form.image_url||null,image_gallery:form.image_gallery||[],notes:form.notes.trim()||null,source:'manual'};const{error}=await supabase.from('fh_wardrobe_items').insert(payload);setSaving(false);if(error)setMessage(error.message);else navigate('/wardrobe')}
 return <main className="page add-item-page"><Header title="Add Wardrobe Item" subtitle="Paste a retailer URL or upload the product image."/><button className="back-link" onClick={()=>navigate('/wardrobe')}><ArrowLeft size={17}/> Back</button><form className="item-form" onSubmit={submit}><section className="form-card"><h2>Import from retailer</h2><label><span className="field-label"><LinkIcon size={14}/> Retailer product URL</span><input type="url" value={form.product_url} onChange={e=>change('product_url',e.target.value)} placeholder="Paste the Torrid, Lane Bryant, BloomChic… product page"/></label><ProductImageManager userId={session?.user?.id} productUrl={form.product_url} onImported={applyImported} imageUrl={form.image_url} gallery={form.image_gallery} onImageChange={v=>change('image_url',v)} onGalleryChange={v=>change('image_gallery',v)}/><p className="form-help">Import tries to pull the retailer's real product details and caches usable product images in For Hollie's storage. If the site blocks that, Upload Image always works.</p></section><section className="form-card"><h2>Item</h2><label>Product name *<input value={form.custom_name} onChange={e=>change('custom_name',e.target.value)} required/></label><div className="form-grid"><label>Retailer<select value={form.retailer_id} onChange={e=>change('retailer_id',e.target.value)}><option value="">Choose retailer</option>{retailers.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></label><label>Brand<input value={form.brand} onChange={e=>change('brand',e.target.value)}/></label></div><div className="form-grid"><label>Category<select value={form.category} onChange={e=>categoryChanged(e.target.value)}>{Object.keys(taxonomy).map(c=><option key={c}>{c}</option>)}</select></label><label>Subcategory<select value={form.subcategory} onChange={e=>change('subcategory',e.target.value)}>{subs.map(s=><option key={s}>{s}</option>)}</select></label></div><div className="form-grid"><label>Color<input value={form.color} onChange={e=>change('color',e.target.value)}/></label><label>Size<input value={form.size} onChange={e=>change('size',e.target.value)} placeholder="3, 24, 50D…"/></label></div></section><section className="form-card"><h2>Purchase</h2><div className="form-grid"><label>Price paid<input type="number" min="0" step="0.01" value={form.purchase_price} onChange={e=>change('purchase_price',e.target.value)}/></label><label>Regular price<input type="number" min="0" step="0.01" value={form.regular_price} onChange={e=>change('regular_price',e.target.value)}/></label></div><div className="form-grid"><label>Purchase date<input type="date" value={form.purchase_date} onChange={e=>change('purchase_date',e.target.value)}/></label><label>Status<select value={form.status} onChange={e=>change('status',e.target.value)}><option value="owned">Owned</option><option value="on_the_way">On the way</option></select></label></div></section><section className="form-card"><h2>Notes</h2><label>Optional notes<textarea value={form.notes} onChange={e=>change('notes',e.target.value)}/></label></section>{message&&<div className="error-note">{message}</div>}<button className="primary-btn save-item-btn" disabled={saving}><Save size={17}/>{saving?'Saving…':'Save to Wardrobe'}</button></form></main>}
