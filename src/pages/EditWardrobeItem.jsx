import { useEffect, useState } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { ArrowLeft, Save, Trash2, Link as LinkIcon } from 'lucide-react'
import Header from '../components/Header'
import ProductImageManager from '../components/ProductImageManager'
import { supabase } from '../lib/supabase'
import { useAuth } from '../context/AuthContext'
import { taxonomy } from '../lib/taxonomy'
import '../wardrobe.css'
export default function EditWardrobeItem(){const{id}=useParams();const{session}=useAuth();const nav=useNavigate();const[item,setItem]=useState(null),[retailers,setRetailers]=useState([]),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');useEffect(()=>{(async()=>{const[{data:i,error},{data:r}]=await Promise.all([supabase.from('fh_wardrobe_items').select('*').eq('id',id).eq('owner_user_id',session.user.id).single(),supabase.from('fh_retailers').select('id,name').eq('enabled',true).order('scan_priority')]);if(error)setMsg(error.message);else setItem({...i,image_gallery:i?.image_gallery||[]});setRetailers(r||[])})()},[id,session?.user?.id]);if(!item)return <main className="page"><Header title="Edit Item"/>{msg?<div className="error-note">{msg}</div>:<div className="empty-panel">Loading…</div>}</main>;const ch=(k,v)=>setItem(p=>({...p,[k]:v}));const applyImported=d=>setItem(p=>({...p,custom_name:d.name||p.custom_name,brand:d.brand||p.brand,category:d.category||p.category,subcategory:taxonomy[d.category]?.[0]||p.subcategory,color:d.color||p.color,regular_price:d.regularPrice??p.regular_price,product_url:d.canonicalUrl||p.product_url,retailer_id:d.retailer?.id||p.retailer_id,image_url:d.images?.[0]||p.image_url,image_gallery:d.images||p.image_gallery}));const save=async e=>{e.preventDefault();setSaving(true);const{error}=await supabase.from('fh_wardrobe_items').update({custom_name:item.custom_name,retailer_id:item.retailer_id||null,brand:item.brand||null,category:item.category,subcategory:item.subcategory||null,color:item.color||null,size:item.size||null,purchase_price:item.purchase_price===''?null:Number(item.purchase_price),regular_price:item.regular_price===''?null:Number(item.regular_price),purchase_date:item.purchase_date||null,status:item.status,product_url:item.product_url||null,image_url:item.image_url||null,image_gallery:item.image_gallery||[],notes:item.notes||null}).eq('id',id).eq('owner_user_id',session.user.id);setSaving(false);if(error)setMsg(error.message);else nav('/wardrobe')};const del=async()=>{if(!window.confirm('Remove this item from the wardrobe?'))return;await supabase.from('fh_wardrobe_items').delete().eq('id',id).eq('owner_user_id',session.user.id);nav('/wardrobe')};const subs=taxonomy[item.category]||[];return <main className="page add-item-page"><Header title="Edit Wardrobe Item"/><button className="back-link" onClick={()=>nav('/wardrobe')}><ArrowLeft size={17}/> Back</button><form className="item-form" onSubmit={save}><section className="form-card"><h2>Product images</h2><label><span className="field-label"><LinkIcon size={14}/> Retailer product URL</span><input type="url" value={item.product_url||''} onChange={e=>ch('product_url',e.target.value)}/></label><ProductImageManager userId={session?.user?.id} productUrl={item.product_url} onImported={applyImported} imageUrl={item.image_url} gallery={item.image_gallery||[]} onImageChange={v=>ch('image_url',v)} onGalleryChange={v=>ch('image_gallery',v)}/></section><section className="form-card"><h2>Item</h2><label>Product name<input value={item.custom_name||''} onChange={e=>ch('custom_name',e.target.value)}/></label><div className="form-grid"><label>Retailer<select value={item.retailer_id||''} onChange={e=>ch('retailer_id',e.target.value)}><option value="">Choose retailer</option>{retailers.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></label><label>Brand<input value={item.brand||''} onChange={e=>ch('brand',e.target.value)}/></label></div><div className="form-grid"><label>Category<select value={item.category||'Tops'} onChange={e=>{ch('category',e.target.value);ch('subcategory',taxonomy[e.target.value]?.[0]||'')}}>{Object.keys(taxonomy).map(c=><option key={c}>{c}</option>)}</select></label><label>Subcategory<select value={item.subcategory||''} onChange={e=>ch('subcategory',e.target.value)}>{subs.map(s=><option key={s}>{s}</option>)}</select></label></div><div className="form-grid"><label>Color<input value={item.color||''} onChange={e=>ch('color',e.target.value)}/></label><label>Size<input value={item.size||''} onChange={e=>ch('size',e.target.value)}/></label></div></section><section className="form-card"><h2>Purchase</h2><div className="form-grid"><label>Price paid<input type="number" step="0.01" value={item.purchase_price??''} onChange={e=>ch('purchase_price',e.target.value)}/></label><label>Regular price<input type="number" step="0.01" value={item.regular_price??''} onChange={e=>ch('regular_price',e.target.value)}/></label></div><div className="form-grid"><label>Purchase date<input type="date" value={item.purchase_date||''} onChange={e=>ch('purchase_date',e.target.value)}/></label><label>Status<select value={item.status} onChange={e=>ch('status',e.target.value)}><option value="owned">Owned</option><option value="on_the_way">On the way</option><option value="returned">Returned</option><option value="donated">Donated</option><option value="retired">Retired</option></select></label></div><label>Notes<textarea value={item.notes||''} onChange={e=>ch('notes',e.target.value)}/></label></section>{msg&&<div className="error-note">{msg}</div>}<div className="form-actions"><button className="secondary-btn danger" type="button" onClick={del}><Trash2 size={16}/> Remove</button><button className="primary-btn" disabled={saving}><Save size={16}/>{saving?'Saving…':'Save Changes'}</button></div></form></main>}
